import type { API, DynamicPlatformPlugin, Logger, PlatformAccessory, PlatformConfig } from 'homebridge';
import { TTLockAccessory } from './lockAccessory';
import type { TTLockPlatformConfig } from './types';
export declare class TTLockPlatform implements DynamicPlatformPlugin {
    readonly log: Logger;
    readonly api: API;
    readonly Service: any;
    readonly Characteristic: any;
    readonly accessories: Map<string, PlatformAccessory<Record<string, unknown>>>;
    readonly activeLocks: Map<number, TTLockAccessory>;
    readonly config: Required<TTLockPlatformConfig>;
    private readonly apiClient;
    private pollTimer?;
    private launchCompleted;
    private pollCycleInFlight?;
    private pollingPauseCount;
    constructor(log: Logger, config: PlatformConfig, api: API);
    configureAccessory(accessory: PlatformAccessory): void;
    runWithPollingPaused<T>(operation: () => Promise<T>): Promise<T>;
    refreshAllLockStates(): Promise<void>;
    refreshAllBatteries(): Promise<void>;
    private didFinishLaunching;
    private discoverLocksOnce;
    private runPollingCycle;
    private runPollingCycleInternal;
    private syncAccessoriesFromLocks;
    private startPoller;
    private pausePolling;
    private resumePolling;
    private isPollingPaused;
    private clearPollingTimer;
    private schedulePoll;
    private runScheduledPollCycle;
    private normalizeConfig;
    private isConfigured;
    private numberOrDefault;
    private parseHideLocks;
    private errorToMessage;
}
